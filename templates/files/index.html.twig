{% extends 'base.html.twig' %}

{% block title %} Files {% endblock %}

{% block body %}
    <div class="album py-5 bg-light">
        <div class="container">
            <h3>Files {{ files|length }}</h3>
            {{ include('files/items.html.twig', { files: files}) }}
        </div>
    </div>
{% endblock %}
